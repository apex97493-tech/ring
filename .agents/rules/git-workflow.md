# Git Push Policy

- **CRITICAL RULE**: NEVER run `git push` or push commits to GitHub unless the user EXPLICITLY issues an instruction to push (e.g. "push this to git" or "push to github").
- All code changes, tests, and builds must remain local in the workspace.
- Always ask or wait for the user's explicit approval before pushing.
